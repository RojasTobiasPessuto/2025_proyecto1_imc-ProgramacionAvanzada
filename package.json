{
  "dependencies": {
    "@tailwindcss/vite": "^4.1.13",
    "dotenv": "^17.2.2",
    "tailwindcss": "^4.1.13"
  },
  "scripts": {
    "postinstall": "npm --prefix backend install --include=dev",
    "build": "npm --prefix backend run build",
    "start": "npm --prefix backend run build && node backend/dist/main.js"
  }
}
